{
  "paths": {
    "input_file": "data/sample_input.csv"
  },

  "schema":{
             "required_columns": [
    "customer_id",
    "first_name",
    "last_name",
    "postcode",
    "created_at"
        ]
     }
}

